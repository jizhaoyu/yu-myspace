你现在是一位顶级全栈架构师 + Java 25 专家 + 桌面应用开发专家。

请从零开始为我完整开发一个名为 **Fangyu Code** 的现代化桌面 AI 编程助手，目标是重现并优化 https://github.com/00OO666/fangyu-code 的核心体验。

### 技术栈要求（必须严格遵守）：
- **JDK**：25（必须使用 Virtual Threads + StructuredTaskScope）
- **后端框架**：Spring Boot 4.0.3 + Krema（Java 版的 Tauri）
- **Krema 集成**：使用 `@KremaCommand` 注解暴露所有后端方法，前端通过 `window.krema.invoke()` 和 `window.krema.on()` 调用
- **前端**：React 18 + TypeScript + Vite + Tailwind CSS 4 + shadcn/ui + Zustand + TanStack Query + Framer Motion
- **数据库**：SQLite（使用 Spring Data JDBC）
- **构建工具**：Maven
- **并发模型**：大量使用 Java 25 Virtual Threads + StructuredTaskScope

### 核心功能要求（必须全部实现）：

1. **智能提示词队列系统（最核心）**
   - 支持用户持续输入提示词，AI 处理时不阻塞输入
   - 支持优先级队列 + 插队机制（高优先级可插入前面）
   - 支持批量提交、暂停、取消、编辑正在等待的任务
   - 实时显示队列状态（待处理 / 处理中 / 已完成）

2. **双 Codex 并行执行 + 互相监督机制（重点实现）**
   - 支持同时运行两个 Codex 任务（例如：一个主生成代码 + 一个并行审查/优化/成本监控）
   - 两个任务必须能**互相看到对方状态和进度**
   - 一方失败、取消或超时，自动影响另一方（取消或降级）
   - 双方可根据对方进度动态调整策略（例如对方太慢则加速自己或插队）

3. **多 AI 引擎切换**
   - 支持 Claude Code（CLI 调用）、OpenAI Codex（API）、Google Gemini
   - 引擎统一接口 + Streaming 输出支持
   - 可随时切换引擎

4. **实时成本与 Token 追踪**
   - 显示当前会话用量、周用量、剩余额度、倒计时
   - 实时成本计算和超预算预警
   - 进度条 + 颜色预警（绿/黄/红）

5. **智能上下文管理**
   - 自动压缩长上下文（摘要 + 关键信息保留）
   - Token 计数实时监控

6. **聊天历史管理**
   - SQLite 存储 + FTS5 全文搜索
   - 支持搜索、删除、导出

7. **其他必要功能**
   - 主题切换、设置页面
   - 托盘图标、开机自启（Krema 支持）
   - 错误处理与日志系统
   - 前端实时事件推送（Krema 事件系统）

### 项目结构要求（请严格按此结构生成）：

### 输出要求：

请按以下顺序逐步输出完整可运行代码：

1. **pom.xml** 完整内容（包含 JDK 25、Spring Boot 4.0.3、Krema 依赖、SQLite 等）
2. **application.yml** 完整配置
3. **FangyuApplication.java**（包含 Spring Boot + Krema 启动逻辑）
4. **krema.toml** 配置
5. **完整目录结构确认**
6. **核心模块代码**（优先输出以下类）：
   - AiEngine 接口 + ClaudeEngine / CodexEngine / GeminiEngine
   - PromptQueueManager（@Service，使用 Virtual Threads）
   - TaskSupervisor（双任务互相监督核心逻辑，使用 StructuredTaskScope）
   - DualCodexTask 示例
   - PromptCommand（@KremaCommand 类，暴露 submitPrompt、cancelTask 等方法）
7. **前端关键文件**（App.tsx、zustand store、主要组件结构）

请确保所有代码：
- 生产可用、结构清晰、注释充分
- 充分利用 Java 25 Virtual Threads
- 正确集成 Spring Boot + Krema
- 双任务监督机制要清晰且健壮

现在请开始输出，从 **pom.xml** 开始。